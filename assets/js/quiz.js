document.querySelectorAll('.mc-quiz').forEach((quiz)=>{const correct=quiz.dataset.correct;const feedback=quiz.querySelector('.mc-feedback');const buttons=quiz.querySelectorAll('.mc-option');buttons.forEach((button)=>button.addEventListener('click',()=>{if(quiz.dataset.answered==='1')return;quiz.dataset.answered='1';const selected=button.dataset.option;const isCorrect=selected===correct;buttons.forEach((item)=>{item.disabled=true;if(item.dataset.option===correct)item.classList.add('correct');});if(!isCorrect)button.classList.add('wrong');feedback.textContent=isCorrect?'✓ Richtig!':'✗ Falsch!';feedback.classList.add(isCorrect?'correct':'wrong');}));});
